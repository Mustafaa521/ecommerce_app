import 'package:flutter/material.dart';

class Dimentions {
  static const EdgeInsets bodyPadding = EdgeInsets.symmetric(horizontal: 16);
  static const  BorderRadius  cardBorder = BorderRadius.all(Radius.circular(10));

}
